# PRD-AI.md - AI 智能日程安排功能需求文档

**文档版本**: v1.0  
**撰写日期**: 2026-02-28  
**Product Owner**: Sprint 2  
**状态**: 待评审

---

## 1. 背景与目标

### 1.1 背景
基于用户调研报告 Section 2.2 "Calendar Todo Integration User Needs"，用户最高层次的需求是：
- **Level 3 (高级)**: AI自动为任务分配最佳时间
- **Level 4 (终极)**: 根据优先级、能量水平自动规划整天

### 1.2 Sprint 2 目标
实现 AI 智能日程安排核心功能，让用户从手动排程中解放出来，提升日程管理效率。

---

## 2. 核心功能

### 2.1 智能时间块分配算法

#### 功能描述
AI 根据任务属性（优先级、预计时长、截止时间）和用户日程状态，自动计算并推荐最佳时间安排。

#### 输入参数
| 参数 | 类型 | 说明 |
|------|------|------|
| 任务优先级 | Enum | 高 / 中 / 低 |
| 预计时长 | Number | 分钟为单位 |
| 截止时间 | DateTime | 可选，任务必须完成的最后期限 |
| 任务类型 | Enum | 专注型 / 事务型 / 创意型 / 会议型 |
| 用户偏好 | Object | 早晨/下午/晚上效率时段偏好 |

#### 输出结果
- 推荐的时间段列表（按匹配度排序）
- 每个推荐的置信度分数
- 冲突检测报告

#### 算法逻辑
```
1. 获取用户空闲时间块
2. 根据任务类型匹配合适时段：
   - 专注型任务 → 长连续空闲块 + 高效时段
   - 事务型任务 → 短间隙时间
   - 创意型任务 → 用户偏好的创意时段
   - 会议型任务 → 标准工作时间
3. 考虑截止时间进行倒排
4. 高优先级任务优先占据黄金时段
5. 输出最优安排方案
```

---

### 2.2 用户偏好学习（高效时段）

#### 功能描述
通过分析用户的历史行为数据，学习其高效工作时段和任务完成模式，持续优化排程建议。

#### 学习维度
| 维度 | 描述 | 数据来源 |
|------|------|----------|
| 高效时段 | 用户完成高质量工作的时间段 | 任务完成时间、自我反馈评分 |
| 任务耗时规律 | 同类型任务的实际完成时间 | 历史任务实际 vs 预估时长 |
| 打断容忍度 | 用户在不同时间对打断的接受度 | 日程密度、重新排程频率 |
| 工作节奏 | 偏好集中工作 vs 分散短任务 | 任务完成分布模式 |

#### 偏好配置
```typescript
interface UserPreference {
  // 时段偏好 (0-100 分数)
  productiveHours: {
    morning: number;    // 06:00-12:00
    afternoon: number;  // 12:00-18:00
    evening: number;    // 18:00-22:00
    night: number;      // 22:00-06:00
  };
  
  // 任务类型偏好
  taskPreferences: {
    deepWork: PreferredSlot[];      // 专注工作偏好时段
    adminWork: PreferredSlot[];     // 行政事务偏好时段
    creativeWork: PreferredSlot[];  // 创意工作偏好时段
  };
  
  // 缓冲设置
  bufferTime: number;  // 任务间默认缓冲时间（分钟）
  maxDailyTasks: number;  // 每日最大任务数建议
}
```

#### 冷启动策略
新用户首次使用时：
1. 引导填写基础偏好问卷（5个问题）
2. 使用默认保守算法
3. 7天后根据初步数据微调
4. 30天后启用完整学习模型

---

### 2.3 冲突自动检测与重新安排

#### 功能描述
实时监测日历变化，当检测到冲突时自动触发重新排程，并向用户提供智能调整建议。

#### 冲突类型
| 冲突类型 | 描述 | 处理方式 |
|----------|------|----------|
| 时间重叠 | 两个事件占用同一时间 | 自动重新安排低优先级任务 |
| 缓冲不足 | 任务间没有足够的过渡时间 | 建议调整时间或发出预警 |
| 截止时间风险 | 任务可能无法按期完成 | 提前预警 + 建议调整方案 |
| 过载警告 | 单日任务量超出合理范围 | 建议任务拆分或延后 |

#### 自动重新排程流程
```
触发条件: 日历事件变更 || 新任务添加 || 截止时间临近

流程:
1. 扫描未来7天日程
2. 识别冲突项
3. 计算重新排程方案:
   a. 优先保留固定事件（会议、约会）
   b. 低优先级任务为可移动项
   c. 寻找替代时间槽
4. 生成调整建议列表
5. 通知用户确认（高影响）或自动执行（低影响）
```

#### 重新排程策略
```typescript
interface RescheduleStrategy {
  // 优先级权重
  priorityWeight: {
    high: 100,      // 尽量不移动
    medium: 50,     // 可移动
    low: 10,        // 优先移动
  };
  
  // 截止时间容忍度
  deadlineBuffer: {
    hard: 0,        // 硬截止，不可延期
    soft: 86400000, // 软截止，可延期1天
    flexible: null, // 灵活截止，可自由调整
  };
  
  // 移动限制
  constraints: {
    maxDaysForward: 7,    // 最多向后推迟7天
    keepTimeOfDay: true,  // 尽量保持相同时段
    respectPreferences: true, // 尊重用户偏好
  };
}
```

---

### 2.4 任务优先级与截止时间平衡

#### 功能描述
综合考虑任务优先级和截止时间，智能平衡两者在排程中的权重，避免"紧急但不重要"任务挤占"重要但不紧急"任务的时间。

#### 优先级模型
```
任务得分 = (优先级权重 × 优先级) + (时间紧迫度 × 截止时间)

优先级权重:
- 高: 100
- 中: 50
- 低: 25

时间紧迫度计算:
- 剩余时间 < 1天: 100
- 剩余时间 1-3天: 75
- 剩余时间 3-7天: 50
- 剩余时间 > 7天: 25
- 无截止时间: 10
```

#### 艾森豪威尔矩阵集成
| 象限 | 特征 | 排程策略 |
|------|------|----------|
| 重要且紧急 | 高优先级 + 近截止 | 立即安排，黄金时段 |
| 重要不紧急 | 高优先级 + 远截止 | 预留高质量时间块 |
| 紧急不重要 | 低优先级 + 近截止 | 间隙时间或委派 |
| 不紧急不重要 | 低优先级 + 远截止 | 延后或删除 |

#### 动态调整
系统每日自动评估：
1. 检查即将到期的重要任务
2. 提醒用户关注"重要不紧急"任务
3. 建议将"紧急不重要"任务委派或简化

---

## 3. 用户故事

### 3.1 用户输入任务后，AI自动建议最佳时间安排

**作为** 忙碌的职场人士  
**我希望** 输入一个任务后，AI能立即推荐最佳的时间段  
**以便** 我不需要手动在日历上寻找合适的时间块

#### 验收标准
- [ ] 用户输入任务（标题、预计时长、优先级）后，1秒内显示推荐时间
- [ ] 推荐列表至少包含3个可选时间段
- [ ] 每个推荐显示置信度分数和推荐理由
- [ ] 用户可一键接受推荐并创建日历事件
- [ ] 若用户拒绝推荐，可手动选择时间或修改偏好

#### 交互流程
```
1. 用户点击"新建任务"
2. 填写任务信息（标题、时长、优先级、截止时间）
3. 点击"获取AI建议"
4. 系统显示推荐时间列表：
   - 推荐1: 明天 09:00-10:30（置信度 95%）
     理由: 这是你处理专注型任务的高效时段
   - 推荐2: 今天下午 14:00-15:30（置信度 78%）
     理由: 空闲时间，但接近你的低效率时段
   - 推荐3: 后天 10:00-11:30（置信度 65%）
     理由: 截止时间前充裕，但会与偏好冲突
5. 用户选择推荐或手动调整
```

---

### 3.2 日历事件冲突时，AI自动重新安排任务

**作为** 日程经常变动的用户  
**我希望** 当有紧急会议插入导致冲突时，AI能自动帮我重新安排被影响的任务  
**以便** 我不需要手动逐一调整任务时间

#### 验收标准
- [ ] 系统实时监测日历变化
- [ ] 检测到冲突后5秒内生成重新排程方案
- [ ] 高影响调整需用户确认，低影响可自动执行
- [ ] 重新排程后发送通知，说明调整内容
- [ ] 保留手动撤销调整的选项

#### 交互流程
```
场景: 用户收到紧急会议邀请，与已安排的任务冲突

1. 会议邀请被接受
2. AI检测到冲突：
   - 固定事件: 紧急会议 14:00-15:00
   - 冲突任务: "撰写周报" 14:30-15:30（可移动）
3. AI生成重新排程方案:
   - 将"撰写周报"移至 16:00-17:00
   - 理由: 该时段为高效时间，且不影响其他任务
4. 系统推送通知:
   "检测到日程冲突，AI已为你重新安排：
    - 紧急会议已添加: 今天 14:00-15:00
    - '撰写周报' 已移至: 今天 16:00-17:00
    点击确认接受，点击查看详情可手动调整"
5. 用户在通知中确认或修改
```

---

### 3.3 根据历史数据学习用户的高效工作时段

**作为** 追求效率的用户  
**我希望** 日历应用能学习我的工作效率模式，知道我在什么时间最适合做什么类型的工作  
**以便** AI排程越来越符合我的个人节奏

#### 验收标准
- [ ] 系统持续收集用户行为数据（任务完成时间、评分反馈）
- [ ] 每周生成个人效率报告，展示学习成果
- [ ] 用户可查看和手动调整学习到的偏好
- [ ] 冷启动期间（7天内）提供合理的默认建议
- [ ] 用户可随时重置学习数据

#### 交互流程
```
1. 新用户引导: 填写5个问题的基础偏好问卷
   - "你通常在什么时间感到最有精力？"
   - "你更喜欢上午处理专注型工作还是下午？"
   - ...

2. 日常使用中的反馈收集:
   - 任务完成后弹出:"这次安排的时间合适吗？😊😐😔"
   - 可选填写具体反馈

3. 每周日推送"效率周报":
   "本周你的效率洞察：
    - 周二上午完成任务最快，平均用时比预估少20%
    - 周五下午任务完成率较低
    - 建议：将重要任务安排在周二上午
    - AI已更新你的偏好设置"

4. 用户可在设置中查看和修改：
   - 我的高效时段
   - 各类型任务的最佳时间
   - 学习效率历史曲线
```

---

## 4. 验收标准汇总

### 4.1 性能标准

| 指标 | 目标值 | 测试方法 |
|------|--------|----------|
| 智能排程响应时间 | ≤ 1秒（10个任务） | 压力测试 |
| 冲突检测延迟 | ≤ 5秒 | 实时同步测试 |
| 重新排程计算时间 | ≤ 2秒 | 复杂场景测试 |
| 算法准确率 | ≥ 85% 用户满意度 | A/B测试+反馈收集 |

### 4.2 功能标准

| 功能 | 验收标准 |
|------|----------|
| 智能时间块分配 | 算法能在1秒内为10个任务生成安排方案 |
| 用户偏好考虑 | 排程必须考虑用户的早晨/下午/晚上时段偏好 |
| 优先级保证 | 高优先级任务必须优先获得高质量时间块 |
| 冲突处理 | 冲突检测准确率≥95%，误报率≤5% |
| 学习时间 | 7天内达到可用水平，30天内达到最佳水平 |

### 4.3 用户体验标准

| 场景 | 标准 |
|------|------|
| 首次使用 | 新用户在5分钟内完成偏好设置并开始使用 |
| 日常使用 | 单次排程决策不超过3次点击 |
| 通知打扰 | 每日AI通知不超过3条（除非紧急） |
| 可控性 | 所有自动调整均可手动覆盖或撤销 |
| 透明度 | 每个AI建议必须显示推荐理由 |

---

## 5. 技术实现要点

### 5.1 算法架构
- 使用贪心算法 + 启发式搜索的组合策略
- 预留未来接入机器学习模型的接口
- 支持规则引擎，允许高级用户自定义排程规则

### 5.2 数据存储
- 用户偏好数据：本地优先 + 云端同步
- 行为学习数据：匿名化处理，用户可控删除
- 排程历史：保留90天，支持回溯分析

### 5.3 隐私保护
- 任务内容不上传，仅传递时间元数据
- AI计算可在本地执行（可选云端增强）
- 用户完全控制学习数据的收集和使用

---

## 6. 附录

### 6.1 术语表
| 术语 | 定义 |
|------|------|
| 时间块 | 日历上的连续空闲时间段 |
| 黄金时段 | 用户效率最高的时间段 |
| 硬截止 | 不可延期的任务截止时间 |
| 软截止 | 有一定弹性的任务截止时间 |
| 冷启动 | 新用户无历史数据时的初始阶段 |

### 6.2 相关文档
- 用户调研报告: `RESEARCH-ENHANCED.md`
- 产品架构文档: `ARCHITECTURE.md` (待创建)
- API 设计文档: `API-DESIGN.md` (待创建)

---

**文档评审记录**

| 日期 | 评审人 | 意见 | 状态 |
|------|--------|------|------|
| 2026-02-28 | Product Owner | 初始版本 | ✅ 完成 |
| | | | |

---

*本文档由 Product Owner 编写，Sprint 2 核心交付物*
