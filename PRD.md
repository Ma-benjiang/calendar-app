# 产品需求文档 (PRD)

## 任务与待办清单集成模块

**版本**: v1.0  
**日期**: 2026-02-28  
**状态**: 待评审  

---

## 1. 功能背景

### 1.1 现状分析
当前日历应用已具备完善的事件管理功能：
- ✅ 月/周/日三种视图
- ✅ 事件的增删改查
- ✅ 重复事件支持
- ✅ 通知提醒
- ✅ ICS导入导出
- ✅ 跨平台数据同步

### 1.2 用户痛点
1. **工具割裂**：用户需要在日历应用和待办应用之间频繁切换
2. **计划脱节**：待办任务没有与日历时间块关联，导致计划难以执行
3. **进度盲区**：日历只显示"做了什么"，无法追踪"要做什么"的完成状态
4. **时间浪费**：安排任务时间时需手动比对日历空闲时段

### 1.3 机会点
将任务管理深度集成到日历中，实现 **GTD (Getting Things Done)** 工作流：
- 任务可拖拽到日历变成时间块
- 日历事件可一键转为任务
- 在视图中同时显示事件和任务
- 智能推荐任务的最佳安排时间

---

## 2. 用户故事

### 2.1 核心用户画像

| 角色 | 描述 | 核心需求 |
|------|------|---------|
| **职场效率族** | 需要管理多个项目任务，日程紧张 | 任务与时间块联动，避免冲突 |
| **学生党** | 管理作业、考试、课外活动 | 任务截止日自动提醒，进度可视化 |
| **自由职业者** | 自主安排工作时间 | 灵活的任务调度，时间统计分析 |

### 2.2 用户故事清单

#### US-001 创建任务
> 作为用户，我希望能快速创建任务，以便记录需要完成的事项。

**验收标准**:
- 点击"+"或按快捷键`T`可创建任务
- 任务至少包含：标题、截止日期、优先级
- 可选：描述、标签、预计耗时、关联项目

#### US-002 任务与日历关联
> 作为用户，我希望将任务拖拽到日历的某个时间段，以便为任务安排具体的执行时间。

**验收标准**:
- 任务列表中的任务可拖拽到日历视图
- 拖拽后任务变成"已安排"状态，在日历上显示为半透明时间块
- 双击时间块可编辑任务详情
- 任务完成时，对应的时间块显示完成标记

#### US-003 任务视图
> 作为用户，我希望有一个专门的视图查看所有任务，以便按优先级和截止日期管理。

**验收标准**:
- 新增"任务"视图，与月/周/日视图并列
- 支持按：今天/未来7天/本月/已完成 筛选
- 支持按优先级和截止日期排序
- 支持拖拽调整任务顺序（同优先级内）

#### US-004 智能时间建议
> 作为用户，我希望系统能推荐安排任务的最佳时间，以便高效利用碎片时间。

**验收标准**:
- 点击"智能安排"按钮，系统自动扫描日历空闲时段
- 根据任务预计耗时和优先级推荐时间段
- 一键接受建议，自动创建时间块
- 支持设置"深度工作"偏好时段

#### US-005 任务完成追踪
> 作为用户，我希望标记任务完成，以便追踪自己的工作效率。

**验收标准**:
- 任务左侧有复选框，点击标记完成
- 已完成任务显示删除线，移至底部
- 统计视图显示：今日完成数/本周完成数/完成率趋势
- 支持查看历史完成记录

#### US-006 循环任务
> 作为用户，我希望设置周期性重复的任务，以便管理日常习惯。

**验收标准**:
- 支持设置：每天/每周/每月/自定义 重复
- 循环任务实例可单独编辑（不影响其他实例）
- 支持设置"跳过"某个实例
- 连续完成天数统计显示

---

## 3. 详细需求描述

### 3.1 数据模型

```typescript
// 任务实体
interface Task {
  id: string;
  title: string;
  description?: string;
  
  // 时间相关
  dueDate?: Date;           // 截止日期
  scheduledStart?: Date;    // 安排的开始时间（关联日历）
  scheduledEnd?: Date;      // 安排的结束时间
  estimatedMinutes?: number; // 预计耗时（分钟）
  
  // 状态
  status: 'todo' | 'in-progress' | 'completed' | 'cancelled';
  completedAt?: Date;
  
  // 优先级
  priority: 'high' | 'medium' | 'low' | 'none';
  
  // 分类
  project?: string;         // 所属项目
  tags: string[];
  color?: string;
  
  // 循环
  recurrence?: TaskRecurrenceRule;
  parentTaskId?: string;    // 父任务ID（子任务）
  
  // 元数据
  createdAt: Date;
  updatedAt: Date;
}

// 任务循环规则
interface TaskRecurrenceRule {
  frequency: 'daily' | 'weekly' | 'monthly' | 'custom';
  interval?: number;        // 间隔
  weekDays?: number[];      // 周几 [0-6]
  endCondition?: 'never' | 'count' | 'date';
  endCount?: number;
  endDate?: Date;
}
```

### 3.2 功能模块

#### 3.2.1 任务列表 (TaskList)

**位置**: 新增"任务"标签页，或侧边栏常驻

**功能**:
- 快速添加任务输入框（Enter创建，Shift+Enter换行）
- 任务项显示：复选框、标题、截止日期标签、优先级指示条
- 支持展开/收起任务详情
- 支持拖拽排序

**视图模式**:
1. **列表视图**: 平铺显示所有任务
2. **看板视图**: 按状态分栏（待办/进行中/已完成）
3. **时间线视图**: 按截止日期横向排列

#### 3.2.2 任务-日历联动

**日历→任务**:
- 日历事件右键菜单："转为任务"
- 转换后保留时间信息，添加任务属性

**任务→日历**:
- 任务列表中任务可拖拽到日历
- 拖拽时显示预计耗时占用的时间块预览
- 放置后自动设置 scheduledStart/End

**冲突检测**:
- 任务安排时间与其他事件重叠时，显示警告图标
- 提供"查找空闲时间"快捷操作

#### 3.2.3 智能安排引擎

**算法逻辑**:
```
输入: 未安排任务列表 + 日历空闲时段
输出: 推荐的任务-时间匹配

规则:
1. 高优先级任务优先安排
2. 尊重用户的"深度工作"偏好时段
3. 预估耗时 <= 空闲时段时长
4. 截止日期临近的任务优先
5. 避免将任务安排在当前时间之前
```

**用户可配置偏好**:
- 深度工作时段（如 09:00-12:00）
- 最小可安排时间段（如 15分钟）
- 任务缓冲时间（如 事件间留5分钟）

#### 3.2.4 统计与洞察

**个人效率看板**:
- 今日任务：已完成 / 总数
- 本周趋势：每日完成任务数折线图
- 项目分布：各项目任务占比饼图
- 拖延分析：逾期未完成任务列表

---

## 4. 验收标准

### 4.1 功能验收

| 编号 | 验收项 | 优先级 | 验收标准 |
|------|--------|--------|---------|
| AC-001 | 任务CRUD | P0 | 能创建、查看、编辑、删除任务，数据持久化 |
| AC-002 | 任务视图 | P0 | 新增任务视图，支持筛选和排序 |
| AC-003 | 拖拽关联 | P0 | 任务可拖拽到日历，生成时间块 |
| AC-004 | 完成标记 | P0 | 任务可标记完成，有视觉反馈 |
| AC-005 | 循环任务 | P1 | 支持设置重复规则，生成实例 |
| AC-006 | 智能安排 | P1 | 系统能推荐任务安排时间 |
| AC-007 | 统计看板 | P1 | 显示任务完成统计和趋势 |
| AC-008 | 子任务 | P2 | 任务支持嵌套子任务 |

### 4.2 性能验收

- 任务列表加载 < 500ms（100条任务）
- 拖拽操作响应 < 100ms
- 智能安排计算 < 2s（30天内，50个任务）

### 4.3 兼容性验收

- Web端：Chrome/Firefox/Safari/Edge 最新两版本
- Desktop端：Windows 10+/macOS 11+/Linux
- Mobile端：iOS 14+/Android 10+

---

## 5. UI/UX 要点

### 5.1 视觉设计

**任务项样式**:
```
┌─────────────────────────────────────────┐
│ ○  │ 任务标题                 │ 📅明天 │
│    │ #标签                    │ 🔴    │
└─────────────────────────────────────────┘
```
- 复选框：未完成为空心圆，完成为实心+对勾
- 优先级：左侧色条（高=红，中=黄，低=蓝，无=灰）
- 已完成的任务：50%透明度，标题删除线

**日历中的任务时间块**:
- 边框使用任务颜色
- 背景色为任务颜色的15%透明度
- 显示任务图标 ⏱ 区分普通事件

### 5.2 交互设计

**快捷操作**:
| 快捷键 | 功能 |
|--------|------|
| `T` | 快速创建任务（聚焦到任务输入框） |
| `Space` | 选中任务时，切换完成状态 |
| `E` | 编辑选中任务 |
| `D` | 删除选中任务（需确认） |
| `Cmd/Ctrl + Shift + A` | 打开智能安排 |

**拖拽交互**:
- 拖拽任务时，任务项变为半透明卡片跟随鼠标
- 进入日历区域时，显示可放置的时段高亮
- 释放后，任务"飞入"对应时间块位置

### 5.3 空状态设计

**无任务时**:
- 显示插画 + 文案："今天没有任务，享受自由时光吧 🎉"
- 提供"创建第一个任务"按钮

**无搜索结果时**:
- 显示："没有找到匹配的任务"
- 提供"清除筛选"按钮

---

## 6. 技术建议

### 6.1 架构建议

**新增模块**:
```
packages/
├── core/
│   ├── src/
│   │   ├── tasks.ts           # 任务核心逻辑
│   │   ├── taskRecurrence.ts  # 任务循环规则
│   │   ├── smartSchedule.ts   # 智能安排算法
│   │   └── taskStats.ts       # 任务统计
│   └── tests/
│       └── tasks.test.ts
├── ui/
│   ├── src/
│   │   ├── TaskView.tsx       # 任务主视图
│   │   ├── TaskList.tsx       # 任务列表组件
│   │   ├── TaskItem.tsx       # 任务项组件
│   │   ├── TaskForm.tsx       # 任务编辑表单
│   │   ├── TaskStats.tsx      # 统计看板
│   │   ├── SmartSchedule.tsx  # 智能安排弹窗
│   │   └── useTasks.ts        # 任务管理Hook
```

### 6.2 存储方案

**扩展现有StorageManager**:
```typescript
// 新增存储键
const TASKS_STORAGE_KEY = 'calendar_tasks';
const TASK_STATS_KEY = 'calendar_task_stats';

// 保存任务
async saveTasks(tasks: Task[]): Promise<void>
async loadTasks(): Promise<Task[]>
```

**数据迁移**:
- 首次加载时检查是否有任务数据
- 无数据时初始化空数组
- 版本标记以便未来迁移

### 6.3 关键算法实现

**智能安排算法伪代码**:
```typescript
function suggestTimeSlots(tasks: Task[], events: CalendarEvent[], preferences: UserPreferences): Suggestion[] {
  const suggestions: Suggestion[] = [];
  
  // 1. 获取未来30天所有空闲时段
  const freeSlots = getFreeSlots(events, 30);
  
  // 2. 按优先级和截止日期排序任务
  const sortedTasks = tasks
    .filter(t => !t.scheduledStart)  // 只取未安排的任务
    .sort((a, b) => {
      const priorityScore = { high: 3, medium: 2, low: 1, none: 0 };
      const scoreA = priorityScore[a.priority] * 10 + getDaysUntilDue(a);
      const scoreB = priorityScore[b.priority] * 10 + getDaysUntilDue(b);
      return scoreB - scoreA;
    });
  
  // 3. 为每个任务找最佳时段
  for (const task of sortedTasks) {
    const estimatedDuration = task.estimatedMinutes || 30;
    const bestSlot = findBestSlot(freeSlots, estimatedDuration, preferences);
    
    if (bestSlot) {
      suggestions.push({
        taskId: task.id,
        suggestedStart: bestSlot.start,
        suggestedEnd: new Date(bestSlot.start.getTime() + estimatedDuration * 60000)
      });
      
      // 从空闲时段中移除已分配的
      removeSlot(freeSlots, bestSlot);
    }
  }
  
  return suggestions;
}
```

### 6.4 第三方库建议

| 功能 | 推荐库 | 理由 |
|------|--------|------|
| 拖拽排序 | @dnd-kit/core | 现代化，React友好，无障碍支持好 |
| 日期计算 | date-fns | 模块化，tree-shake友好 |
| 图表 | recharts | React原生，易定制 |

### 6.5 测试策略

**单元测试**:
- TaskManager CRUD 操作
- 智能安排算法边界条件
- 任务循环规则生成

**集成测试**:
- 任务拖拽到日历的完整流程
- 数据持久化与加载

**E2E测试**:
- 创建任务 → 安排时间 → 标记完成的完整用户旅程

---

## 7. 发布计划

### Phase 1: MVP (2周)
- 基础任务CRUD
- 任务列表视图
- 任务-日历拖拽关联

### Phase 2: 增强 (2周)
- 循环任务
- 任务统计看板
- 快捷键支持

### Phase 3: 智能 (2周)
- 智能安排算法
- 时间建议
- 用户偏好设置

---

## 8. 附录

### 8.1 术语表

| 术语 | 定义 |
|------|------|
| GTD | Getting Things Done，一种任务管理方法 |
| 时间块 | 日历上为任务预留的具体时间段 |
| 深度工作 | 需要高度专注、无干扰的工作模式 |
| 循环任务 | 按规则周期性重复的任务 |

### 8.2 参考产品

- Todoist - 任务管理标杆
- Notion Calendar - 日历与任务结合
- Akiflow - 智能时间规划
- Sunsama - 每日计划工作流

---

*文档结束*
